\import Arith.Nat
\import Order.LinearOrder

\instance FinOrder (n : Nat) : LinearOrder.Dec (Fin n) 
  | < => \lam i j => i NatSemiring.< j
  | <-irreflexive => {?hidden_proof}
  | <-transitive => {?hidden_proof}
  | trichotomy => {?hidden_proof}