\import Algebra.Group
\import Algebra.Group.GroupHom
\import Algebra.Monoid
\import Algebra.Ring
\import Algebra.Ring.Factor
\import Algebra.Ring.RingCategory
\import Algebra.Ring.RingHom
\import Category
\import Category.Subcat
\import Function (IsInj, IsSurj)
\import Function.Meta
\import Logic
\import Meta
\import Paths
\import Paths.Meta
\import Relation.Equivalence

\func ringKerImageHom {R : CRing} {S : CRing} (f : RingHom R S) : RingHom (FactorRing (RingHom.KernelC f)) f.Image.IRing \cowith
  | func (x : FactorRing (RingHom.KernelC f)) : f.Image.IRing \with
    | in~ a => (f a, inP (a, idp))
    | ~-equiv x y r => ext $ inv $ rewrite (zro-left, +-assoc, negative-left, zro-right) in pmap (S.+ f y) (rewrite r in f.func-minus {x} {y})
  | func-+ => {?hidden_proof}
  | func-ide => {?hidden_proof}
  | func-* => {?hidden_proof}
  \where {
    \protected \lemma isSurj : IsSurj (func {R} {S} {f}) => {?hidden_proof}

    \protected \lemma isInj : IsInj (func {R} {S} {f}) => {?hidden_proof}
  }


\lemma ringKerImageHom-iso {R S : CRing} (f : RingHom R S) : Iso {CRingCat} {FactorRing (RingHom.KernelC f)} {(RingHom.ImageC f).ICRing} (ringKerImageHom f) => {?hidden_proof}