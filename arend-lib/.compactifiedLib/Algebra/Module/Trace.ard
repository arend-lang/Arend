\import Algebra.Linear.Matrix
\import Algebra.Module
\import Algebra.Module.LinearMap
\import Algebra.Monoid
\import Algebra.Ring
\import Data.Array
\import Function.Meta
\import Logic
\import Meta
\import Paths
\import Paths.Meta
\import Set

\func Trace {R : Ring} {n : Nat} : LinearMap (MatrixModule R n n) (RingLModule R) \cowith
  | func => \lam M => BigSumHom (diag M)
  | func-+ => {?hidden_proof}
  | func-*c => {?hidden_proof}
  \where {
    \func diag : LinearMap (MatrixModule R n n) (ArrayLModule n (RingLModule R)) \cowith
      | func => \lam M => \lam i => M i i
      | func-+ => {?hidden_proof}
      | func-*c => {?hidden_proof}

    \func BigSumHom : LinearMap (ArrayLModule n (RingLModule R)) (RingLModule R) \cowith
      | func => \lam e => R.BigSum e
      | func-+ => {?hidden_proof}
      | func-*c => {?hidden_proof}
  }


\lemma Trace-transpose {R : Ring} {n : Nat} (M : MatrixModule R n n) : Trace M = Trace (transpose M) => {?hidden_proof}

\open MatrixRing

\func Trace-prod {R : CRing} {n m : Nat} (A : MatrixModule R n m) (B : MatrixModule R m n) : Trace (A product B) = Trace (B product A) 
  => rewrite (Trace-prod-unfold A B, R.BigSum-transpose, Trace-prod-unfold B A, help) idp
  \where {
    \protected \lemma help : R.BigSum (\lam j => R.BigSum (\lam k => A k j R.* B j k)) = R.BigSum (\lam i => R.BigSum (\lam j => B i j R.* A j i)) => {?hidden_proof}

    \protected \lemma Trace-prod-unfold {R : Ring} {n m : Nat} (X : MatrixModule R n m) (Y : MatrixModule R m n) :
      Trace (X product Y) = R.BigSum (\lam i => R.BigSum (\lam j => X i j R.* Y j i)) => {?hidden_proof}
  }


\lemma Trace-conjugation {R : CRing} {n : Nat} {A B : Matrix R n n} {T : Monoid.Inv B} : Trace (T.inv product A product B) = Trace A => {?hidden_proof}

\lemma Trace-diagonal {R : Ring} {l : Array R} : Trace (diagonal l) = R.BigSum l => {?hidden_proof}
  \where {
    \lemma diag-diagonal {n : Nat} (l : Array R n) : Trace.diag (diagonal l) = l => {?hidden_proof}
  }


\lemma Trace-ide {R : Ring} {n : Nat} : Trace (Ring.ide {MatrixRing R n}) = R.natCoef n => {?hidden_proof}