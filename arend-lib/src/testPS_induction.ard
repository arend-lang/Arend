\import Arith.Nat
\import Paths
\import Paths.Meta
\open Nat

\lemma plus-suc (a b : Nat) : a + suc b = suc (a + b)
  => idp

\lemma plus-suc-left (a b : Nat) : suc a + b = suc (a + b) \elim b
  | 0 => idp
  | suc b => pmap suc (plus-suc-left a b)
