{- | Functors between precategories, natural transformations, and the functor category construction.
 -
 -   A `Functor` is a map between precategories preserving identities and composition; this module bundles the structure
 -   as a class so that functors can be composed, restricted to opposite categories, and lifted to isomorphisms. Natural
 -   transformations are recorded as the `NatTrans` record with a naturality square, and together they yield the functor
 -   (pre)category `FunctorPrecat` / `FunctorCat`, whose univalence is established when the target is a category. The
 -   module also provides the diagonal family precategory `FamPrecat` and refinements of functors by
 -   faithfulness/fullness, where `FullyFaithfulFunctor` exposes a two-sided inverse on hom-sets that respects
 -   identities and composition.
 -}

\import Algebra.Meta
\import Category
\import Equiv
\import Function.Meta
\import Logic.Meta
\import Meta
\import Paths
\import Paths.Meta

{- | Class of functors between precategories `C` and `D` with object map `F : C -> D`, morphism map `Func`, and the laws
 -   `Func-id` and `Func-o`.
 -}
\class Functor (C : Precat) (D : Precat) (\classifying F : C -> D) {
  | Func {X Y : C} : Hom X Y -> Hom (F X) (F Y)
  | Func-id {X : C} : Func (id {_} {X}) = id
  | Func-o {X Y Z : C} {g : Hom Y Z} {f : Hom X Y} : Func (g ∘ f) = Func g ∘ Func f

  -- | The induced functor `C.op -> D.op`.
  \protected \func op : Functor C.op D.op F \cowith
    | Func f => Func f
    | Func-id => Func-id
    | Func-o => Func-o

  \lemma Func-splitMono (e : SplitMono {C}) : SplitMono (Func e.f) (Func e.hinv) \cowith
    | hinv_f => inv Func-o *> pmap Func e.hinv_f *> Func-id

  -- | Functors send isomorphisms to isomorphisms.
  \lemma Func-iso (e : Iso {C}) : Iso (Func e.f) (Func e.hinv) \cowith
    | SplitMono => Func-splitMono e
    | f_hinv => inv Func-o *> pmap Func e.f_hinv *> Func-id
} \where {
  -- | Computes `coe` along simultaneous paths in source objects of `F` and `G` in terms of a naturality-style square.
  \lemma transport_Hom {C D E : Precat} {x1 y1 : C} (p1 : x1 = y1) {x2 y2 : D} (p2 : x2 = y2) (F : Functor C E) (G : Functor D E) {g : Hom (F x1) (G x2)} {f : Hom (F y1) (G y2)}
                       (h : transport (Hom (G x2)) (pmap G p2) id ∘ g = f ∘ transport (Hom (F x1)) (pmap F p1) id)
    : coe (\lam i => Hom (F (p1 @ i)) (G (p2 @ i))) g right = f \elim p1, p2
    | idp, idp => inv id-left *> h *> id-right

  -- | Transport in `Hom z (F -)` along a path in `C` equals composition with `F` applied to the transported identity.
  \lemma transport_Hom-right {C D : Precat} {x y : C} (p : x = y) {z : D} (F : Functor C D) {g : Hom z (F x)}
    : transport (Hom z) (pmap F p) g = F.Func (transport (Hom x) p id) ∘ g \elim p
    | idp => inv (pmap (∘ g) Func-id *> id-left)

  -- | When `C` is a category, the transport along `isotoid e` reduces to post-composition with `F.Func e.f`.
  \lemma transport_Hom_iso-right {C : Cat} {D : Precat} (e : Iso {C}) {z : D} (F : Functor C D) {g : Hom z (F e.dom)}
    : transport (Hom z) (pmap F (C.isotoid e)) g = F.Func e.f ∘ g
    => transport_Hom-right (C.isotoid e) F *> cong (C.transport_iso e)

  {- | Two-sided version: transport along isomorphism-induced paths in both `C` and `D` reduces to a naturality
   -   equation.
   -}
  \lemma transport_Hom_iso {C D : Cat} {E : Precat} (e1 : Iso {C}) (e2 : Iso {D}) (F : Functor C E) (G : Functor D E) {g : Hom (F e1.dom) (G e2.dom)} {f : Hom (F e1.cod) (G e2.cod)}
                           (h : G.Func e2.f ∘ g = f ∘ F.Func e1.f)
    : coe (\lam i => Hom (F (C.isotoid e1 @ i)) (G (D.isotoid e2 @ i))) g right = f
    => transport_Hom (C.isotoid e1) (D.isotoid e2) F G (pmap (∘ g) (transport_Hom_iso-right e2 G *> id-right) *> h *> pmap (f ∘) (inv (transport_Hom_iso-right e1 F *> id-right)))
}

-- | Identity functor on a precategory.
\func Id {C : Precat} : Functor C C \cowith
  | F X => X
  | Func h => h
  | Func-id => idp
  | Func-o => idp

-- | Composition of functors `G ∘ F`.
\func Comp {C D E : Precat} (G : Functor D E) (F : Functor C D) : Functor C E \cowith
  | F X => G (F X)
  | Func f => G.Func (F.Func f)
  | Func-id => pmap G.Func F.Func-id *> G.Func-id
  | Func-o => pmap G.Func F.Func-o *> G.Func-o

-- | Constant functor sending every object to a fixed `d : D` and every morphism to `id d`.
\func Const {C D : Precat} (d : D) : Functor C D \cowith
  | F _ => d
  | Func _ => id
  | Func-id => idp
  | Func-o => inv id-left
  \where {
    -- | A morphism `f : d -> d'` lifted to a natural transformation between constant functors.
    \func natTrans {C D : Precat} {d d' : D} (f : Hom d d') : NatTrans {C} (Const d) (Const d') \cowith
      | trans _ => f
      | natural g => id-right *> inv id-left
  }

{- | Record of natural transformations `F => G` between functors `C -> D`, with components `trans` and the naturality
 -   square `natural`.
 -}
\record NatTrans {C : Precat} {D : Precat} (F G : Functor C D) {
  | \coerce trans (X : C) : Hom (F X) (G X)
  | natural {X Y : C} (f : Hom X Y) : trans Y ∘ F.Func f = G.Func f ∘ trans X

  -- | The opposite natural transformation `G.op => F.op`.
  \protected \func op : NatTrans G.op F.op \cowith
    | trans => trans
    | natural f => inv (natural f)

  \lemma iso-inv (e : \Pi {X : C} -> Iso (trans X)) : NatTrans G F (\lam X => e.hinv) \cowith
    | natural f => e.isMono $ rewrite (inv o-assoc, e.f_hinv, id-left, inv o-assoc, natural f, o-assoc, e.f_hinv, id-right) idp

  -- | A componentwise-iso natural transformation has an inverse natural transformation `G => F`.
  \lemma iso (e : \Pi {X : C} -> Iso (trans X)) : Iso {FunctorPrecat C D} \this (iso-inv e) \cowith
    | hinv_f => exts \lam x => e.hinv_f
    | f_hinv => exts \lam x => e.f_hinv
} \where {
  {- | Whiskering a natural transformation `α : F => G` on the right by a functor `H : C -> D`, yielding
   -   `Comp F H => Comp G H`.
   -}
  \func Comp-left {C D E : Precat} (a : NatTrans {D} {E}) (H : Functor C D) : NatTrans (Comp a.F H) (Comp a.G H) \cowith
    | trans x => a (H x)
    | natural f => a.natural (H.Func f)

  -- | Whiskering on the left by a functor `H : D -> E`, yielding `Comp H F => Comp H G`.
  \func Comp-right {C D E : Precat} (H : Functor D E) (a : NatTrans {C} {D}) : NatTrans (Comp H a.F) (Comp H a.G) \cowith
    | trans x => H.Func (a x)
    | natural f => inv H.Func-o *> pmap H.Func (a.natural f) *> H.Func-o

  \lemma equals-mono {C D : Precat} {F G : Functor C D} {a b : NatTrans F G} (e : SplitMono {C}) (p : a e.cod = b e.cod) : a e.dom = b e.dom
    => (G.Func-splitMono e).isMono $ inv (a.natural e.f) *> pmap (∘ _) p *> b.natural e.f

  \protected \func \fixl 8 id {C : Precat} {D : Precat} {F : Functor C D} : NatTrans F F \cowith
    | trans X => D.id
    | natural f => id-left *> inv id-right

  \protected \func \fixl 8 compose {C : Precat} {D : Precat} {F G H : Functor C D} (b : NatTrans G H) (a : NatTrans F G) : NatTrans F H \cowith
    | trans X => b X ∘ a X
    | natural f => run {
      rewrite (o-assoc, natural {a} f),
      rewriteI o-assoc,
      rewrite (natural {b} f),
      o-assoc
    }
}

{- | Precategory of functors `C -> D` between small precategories, with natural transformations as morphisms and
 -   pointwise composition.
 -}
\instance FunctorPrecat (C : Precat) (D : Precat) : Precat (Functor C D)
  | Hom => NatTrans
  | id => NatTrans.id
  | o => NatTrans.compose
  | id-left => exts (\lam X => id-left)
  | id-right => exts (\lam X => id-right)
  | o-assoc => exts (\lam X => o-assoc)
  \where {
    -- | Extracts the pointwise iso in `D` from an iso of functors in the functor category.
    \func mapIso {C : Precat} {D : Precat} (e : Iso {FunctorPrecat C D}) (X : C) : Iso {D} \cowith
      | dom => e.dom X
      | cod => e.cod X
      | f => e.f X
      | hinv => e.hinv X
      | hinv_f => path (\lam i => trans {e.hinv_f @ i} X)
      | f_hinv => path (\lam i => trans {e.f_hinv @ i} X)

    \lemma fromIso {C : Precat} {D : Precat} {F G : Functor C D} (f : NatTrans F G) (e : \Pi {x : C} -> Iso (f x))
      : Iso {FunctorPrecat C D} f \cowith
      | hinv => NatTrans.iso-inv e
      | hinv_f => exts \lam x => e.hinv_f
      | f_hinv => exts \lam x => e.f_hinv
  }

-- | Upgrade of `FunctorPrecat` to a category when `D` is a (small) category, providing univalence.
\instance FunctorCat (C : Precat) (D : Cat) : Cat (Functor C D)
  | Precat => FunctorPrecat C D
  | univalence => Cat.makeUnivalence $ later \lam e =>
    (exts (\lam X => D.isotoid (FunctorPrecat.mapIso e X), \lam {X} {Y} h => Cat.transport_Hom_iso (FunctorPrecat.mapIso e X) (FunctorPrecat.mapIso e Y) (natural h)),
     simp_coe $ simp_coe \lam X => Cat.transport_iso (FunctorPrecat.mapIso e X))

-- | Precategory of `J`-indexed families `J -> D` with pointwise morphisms and composition.
\instance FamPrecat {J : \Type} (D : Precat) : Precat (J -> D)
  | Hom F G => \Pi {j : J} -> Hom (F j) (G j)
  | id => id
  | o g f {j} => g ∘ f
  | id-left => ext (\lam {j} => id-left)
  | id-right => ext (\lam {j} => id-right)
  | o-assoc => ext (\lam {j} => o-assoc)

-- | Categorical refinement of `FamPrecat` when `D` is a category, with pointwise univalence.
\instance FamCat {J : \Type} (D : Cat) : Cat (J -> D)
  | Precat => FamPrecat D
  | univalence => Cat.makeUnivalence $ later \lam e => (ext \lam j => D.isotoid (mapIso e j), simp_coe \lam {j} => Cat.transport_iso (mapIso e j))
  \where {
    -- | Extracts the pointwise iso in `D` from an iso in the family category.
    \func mapIso {J : \Type} {D : Precat} (e : Iso {FamPrecat {J} D}) (j : J) : Iso {D} \cowith
      | dom => e.dom j
      | cod => e.cod j
      | f => e.f
      | hinv => e.hinv
      | hinv_f => path (e.hinv_f @ __)
      | f_hinv => path (e.f_hinv @ __)
  }

-- | Extends `Functor` with `isFaithful`: injectivity of `Func` on hom-sets.
\class FaithfulFunctor \extends Functor {
  | isFaithful {X Y : C} {f g : Hom X Y} : Func f = Func g -> f = g

  \protected \func op : FaithfulFunctor \cowith
    | Functor => Functor.op
    | isFaithful p => isFaithful p
}

-- | Extends `Functor` with `isFull`: surjectivity (up to truncation) of `Func` on hom-sets.
\class FullFunctor \extends Functor {
  | isFull {X Y : C} (f : Hom (F X) (F Y)) : ∃ (g : Hom X Y) (Func g = f)

  \protected \func op : FullFunctor \cowith
    | Functor => Functor.op
    | isFull f => isFull f
}

{- | Extends both `FullFunctor` and `FaithfulFunctor` by requiring `Func` to be an `Equiv`, with derived `isFull` and
 -   `isFaithful`.
 -}
\class FullyFaithfulFunctor \extends FullFunctor, FaithfulFunctor {
  | isFullyFaithful {X Y : C} : IsEquiv (Func {_} {X} {Y})
  | isFull f => IsEquiv.isSurj isFullyFaithful f
  | isFaithful p => IsEquiv.isInj isFullyFaithful p

  \protected \func op : FullyFaithfulFunctor \cowith
    | Functor => Functor.op
    | isFullyFaithful => isFullyFaithful

  -- | The two-sided inverse `Hom (F X) (F Y) -> Hom X Y` from the equivalence.
  \protected \func inverse {X Y : C} (f : Hom (F X) (F Y)) : Hom X Y
    => IsEquiv.ret isFullyFaithful f

  -- | Round-trip equations between `Func` and `inverse`.
  \protected \lemma inverse-left {X Y : C} {f : Hom X Y} : inverse (Func f) = f
    => IsEquiv.ret_f isFullyFaithful

  -- | Round-trip equations between `Func` and `inverse`.
  \protected \lemma inverse-right {X Y : C} {f : Hom (F X) (F Y)} : Func (inverse f) = f
    => IsEquiv.f_ret isFullyFaithful

  -- | The inverse sends identity to identity.
  \lemma inverse-id {x : C} : inverse (id {_} {F x}) = id {_} {x}
    => pmap inverse (inv Func-id) *> inverse-left

  -- | The inverse preserves composition.
  \lemma inverse-o {x y z : C} {g : Hom (F y) (F z)} {f : Hom (F x) (F y)} : inverse (g ∘ f) = inverse g ∘ inverse f
    => inv (pmap inverse Func-o *> cong inverse-right inverse-right) *> inverse-left
} \where {
  \func Comp {C D E : Precat} (G : FullyFaithfulFunctor D E) (F : FullyFaithfulFunctor C D) : FullyFaithfulFunctor C E \cowith
    | Functor => Category.Functor.Comp G F
    | isFullyFaithful => IsEquiv.trans F.isFullyFaithful G.isFullyFaithful
}
