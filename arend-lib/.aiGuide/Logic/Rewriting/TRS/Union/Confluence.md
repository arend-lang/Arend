### Logic.Rewriting.TRS.Union.Confluence

Modular confluence for unions of term rewriting systems.

Provides the main confluence theorem for disjoint unions of TRSs, building on the colored framework from `Colors` and embedding results from `Embedding`.
